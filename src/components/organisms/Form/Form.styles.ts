import styled from 'styled-components';

export const Wrapper = styled.form`
	display: flex;
	flex-direction: column;
	padding: 4rem 9.6rem;
	border: 1px solid #f1f1f1;
	box-shadow: -2px 2px 8px rgba(0, 0, 0, 0.256);
`;
